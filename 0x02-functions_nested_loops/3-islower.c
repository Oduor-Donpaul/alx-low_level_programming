#include "main.h"
/**
 * _islower - checks if argument is lowercase
 *
 * Return: 1 if argument is lowercase
 * otherwise, 0
 */
int _islower(int input)
{
	if (input >= 'a' && input <= 'z')
	{
		return 1;
	} else
	{
		return 0;
	}
	return (0);
}
